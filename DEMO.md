# Demo：AI 数据问答实录（DEMO）

这里演示了看板内置 AI 助手的 **3 个真实问题**。每个问题都记录了 AI 的回答、它实际调用的工具、以及**为什么这些数字可信**（回答中的数字与数据库 SQL 结果逐项一致）。

三个问题分别覆盖三种典型查询：**时间汇总**、**门店排行对比**、**单店 + 日期过滤 + 图表联动**。

---

## 问题 1：时间汇总

> **问**：整个 5 月到 7 月，Moneki 总营业额和订单数分别是多少？

**AI 回答**：

> 根据数据库查询结果，2026-05-01 至 2026-07-31 期间：
> - 总营业额：**428,742.00 元**
> - 总订单数：**11,965 单**
> - 平均客单价：35.83 元
> - 退款额：-1,693.00 元
>
> 以上数据来自数据库实查。

**它做了什么**：调用工具 `get_revenue_summary`，参数 `{start_date: "2026-05-01", end_date: "2026-07-31"}`。

**为什么可信**：该工具内部执行的是 `SUM(amount)` / 正金额订单去重等真实 SQL。核对 `db.get_summary()` 返回：`{"revenue": 428742.0, "orders": 11965, "avg_ticket": 35.83, "refund": -1693.0}`，与回答**逐项一致**。

---

## 问题 2：门店排行对比

> **问**：哪家门店营业额最高？第二名呢，差多少？

**AI 回答**：

> 营业额最高的门店是 **Super Tetsudo（日料，上海·黄浦）**，营业额 **89,613.00 元**。
> 第二名是 **Juicy Bao Bao（点心，上海·浦东）**，营业额 **85,776.00 元**。
> 两者差距：89,613.00 − 85,776.00 = **3,837.00 元**。

**它做了什么**：调用工具 `get_store_ranking`（无参数，按营业额降序返回全部门店）。

**为什么可信**：核对 `db.get_store_ranking()` 前两名分别 `Super Tetsudo 89613`、`Juicy Bao Bao 85776`，差值 3837 正确；且回答明确给出了「从 X 减到 Y」的计算过程，而非含糊的「差挺多」。

---

## 问题 3：单店 + 日期过滤 + 图表联动

> **问**：徐汇店的 Super Souper 六月的营业额是多少？

**AI 回答**：

> Super Souper（S01，拉面，上海·徐汇）2026 年 6 月营业额为 **24,771.00 元**，订单 744 单。

**它做了什么**：调用工具 `get_revenue_summary`，参数 `{start_date: "2026-06-01", end_date: "2026-06-30", store_id: "S01"}`。

**为什么可信**：核对 `db.get_summary("2026-06-01", "2026-06-30", "S01")` 返回 `{"revenue": 24771.0, "orders": 744, ...}`，数字完全一致。

**图表联动**：回答的同时，后端从工具参数推导出联动指令 `focus = {start_date: "2026-06-01", end_date: "2026-06-30", store_id: "S01"}`，前端看板随即自动把日期跳到 6 月、门店切到 Super Souper，KPI / 趋势 / Top10 / 异常面板全部联动刷新。

---

## 数字可信的机制保障（而非运气）

三个问题看似「AI 答对了」，背后的机制才是重点：

1. **Function calling 取数**——模型只能选工具 + 给参数，数字来自工具内的 SQL，拿不到 CSV 原文，无法绕过查询编造。
2. **口径统一**——工具与看板接口共用同一套 DB 函数与异常检测模块，AI 报的数与看板展示的数天生一致。
3. **自动化测试兜底**——集成测试会抽取回答中的数字与数据库逐项断言，一旦模型编造，测试即失败（见 `backend/tests/test_ai_answers.py`）。
4. **关键词兜底 + prompt 约束**——「异常预警」类意图确定性触发工具；异常条数强制引用 `total` 字段，杜绝「自数 items 数错」。