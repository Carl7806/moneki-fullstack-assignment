# AI 使用说明（AI_USAGE）

本文件如实记录这个项目里**用了哪些 AI 工具、怎么把活拆给 AI、AI 哪里出错（尤其编造数字）、我怎么发现和修复、哪些部分坚持自己写**。

---

## 一、用了哪些 AI 工具

| 工具 | 用途 | 接入方式 |
|---|---|---|
| **DeepSeek-Chat**（`deepseek-chat`） | 看板内置 AI 问答助手：意图理解、工具选择、自然语言生成 | 通过 OpenAI 兼容 SDK 接入，`base_url = https://api.deepseek.com`，用 **function calling** 让模型「选工具 + 给参数」而非「直接报数」 |
| **TRAE IDE 助手** | 代码生成、重构、调试、文档草稿的辅助 | 人机协作：AI 产出初稿，我逐段审查、修正后采用 |

> 关键设计：**应用内的 AI 问答，模型永远无法直接拿到 CSV 原文**。它只能通过 5 个工具（总览 / 门店排行 / 单品 / 趋势 / 异常）去查 SQLite，回答里的每个数字都对应一条真实 SQL 结果。这一步是「防止 AI 编造」的机制性保障，而不是依赖 prompt 自觉。

---

## 二、怎么把任务拆给 AI（真实 prompt）

我把整题拆成「数据 → API → AI 编排 → 前端 → 测试」五块，每一块交给 AI 时都带明确约束和验收标准。

### 1. AI 问答的 system prompt（`backend/ai/chat.py` 中实际使用）

```text
你是连锁餐饮品牌「Moneki」的运营数据分析助手。你只能基于数据库查询工具返回的真实数据来回答，严禁编造、猜测或凭常识虚报任何数字。

数据背景：
- 时间范围：2026-05-01 到 2026-07-31
- 门店（store_id / 名称 / 经营品类 / 地段）：S01 Super Souper（拉面）… S05 Super Tetsudo（日料）
- 商品品类：主食、点心、小食、饮料

回答规则：
1. 每个数字都必须来自工具查询结果。工具返回什么就说什么，不得四舍五入虚构、不得外推编造。
2. 相对时间要换算成绝对日期：「六月」= 2026-06-01 ~ 2026-06-30；「近一个月」= 2026-07-01 ~ 2026-07-31。
3. 判断涨跌时，用工具返回的逐日/前后数字对比，明确说「从 X 涨到 Y」。
4. 若工具返回空数据，或用户问的是数据里不存在的维度，直接回答「数据里没有相关记录」。
5. 用中文回答，金额保留两位小数并带「元」。
```

### 2. 工具声明（function calling 的 schema，节选）

```json
{
  "name": "get_revenue_summary",
  "parameters": {
    "properties": {
      "start_date": {"type": "string", "description": "开始日期 YYYY-MM-DD，可选"},
      "end_date":   {"type": "string", "description": "结束日期 YYYY-MM-DD，可选"},
      "store_id":   {"type": "string", "description": "门店ID，如 S01，可选"}
    }
  }
}
```

### 3. 拆任务时我给 AI 的验收标准（示例）

> 「写一个 get_sales_anomalies 工具：对每家店每日营业额做 z-score 检测，|z|≥2 记异常，返回 {total, items}。`total` 必须是 `len(items)`，两者必须一致，测试要断言这一点。」
>
> 「异常检测逻辑抽到 analytics.py，让 AI 工具和 /api/dashboard/anomalies 接口共用同一个函数，避免两处口径不一致。」

这样给的任务自带「可验证的输出契约」，AI 产出的代码能被测试钉死。

---

## 三、AI 出错在哪里、我怎么发现与修复

### 错误 1：LLM 直接编造营业额数字（最严重）

- **现象**：最初让模型直接回答「总营业额多少」，它会凭训练记忆/常识编一个数字，和真实数据对不上。
- **发现**：把 AI 回答和 `db.get_summary()` 结果一比对就露馅。
- **修复**：改为 function calling——数字只能来自工具里的真实 SQL；再写**集成测试**，抽取回答中的数字与 DB 逐项断言一致。从此「编造」在自动化测试层直接失败。

### 错误 2：异常条数数错

- **现象**：异常工具返回 `{total, items}`，模型有时自己逐个数 `items`，数错总数。
- **发现**：集成测试里 `result["total"] != len(result["items"])` 的断言场景，模型口头报的数对不上。
- **修复**：prompt 明确写「异常条数直接引用 `total`，不要自行数 items 的个数」，并加了对应测试。

### 错误 3：异常/预警意图工具选择漂移

- **现象**：同一个「哪些门店营业额有异常」的问题，模型有时不调用异常工具，而调了趋势/排行工具，答非所问。
- **发现**：确定性测试里多次 FakeClient 跑同一个问题，工具调用列表不稳定。
- **修复**：加**关键词兜底** `wants_anomaly_check()`——命中「异常/预警」类关键词就直接预注入异常工具的真实结果，绕开 LLM 选择的不稳定。

### 错误 4：相对时间换算错误

- **现象**：问「六月营业额」，模型对「六月」的起止日期判断漂移。
- **修复**：prompt 里写死相对时间→绝对日期的映射表（六月 = 06-01~06-30 等）。

### 错误 5：ETL 数据清洗的脏数据（属于数据问题，AI 帮定位、我定规则）

- **现象**：CSV 里日期格式混用（`2026/05/01`、`01-05-2026`）、存在脏外键 `P99`/`S99`、重复行、金额带货币符号、空金额。
- **处理**：AI 帮我扫描出脏数据类型，但**清洗规则和口径由我定死**，并对账 `12131 - 30 - 7 - 79 = 12015` 闭环验证。

---

## 四、哪些部分坚持自己写、为什么

| 模块 | 谁写 | 为什么我坚持自己写 |
|---|---|---|
| 核心指标口径（营业额扣退款、订单数=正金额去重） | 自己 | 这是业务正确性，错一个口径全盘皆错，不能交给 AI 猜 |
| ETL 清洗规则 + 对账公式 | 自己 | 确定性数据逻辑，对账必须可解释、可复现 |
| `db.py` 数据库访问层、`analytics.py` 异常检测 | 自己 | 边界条件多（空值、除零、连接复用），AI 容易在边角出 bug |
| 测试的关键断言（数字一致性） | 自己 | 测试是「数字可信」的最后防线，断言必须精确 |
| 组件样板、CSS、函数抽取等重复性代码 | AI 起草、我审查修改 | 这类活 AI 快且不易出错，审查成本低 |

我的原则：**AI 擅长写"能被测试验证"的代码和样板，但"业务口径、确定性算法、验收断言"这三类必须我拍板并复核**——这决定了这个项目最后经得起「数字为什么可信」的拷问。